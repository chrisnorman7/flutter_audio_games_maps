/// The file extensions of sound files that Synthizer will work with.
const audioFileExtensions = ['.wav', '.mp3'];

/// The key where recent directories are stored.
const recentDirectoriesKey = 'map_editor_recent_directories';

/// The filename for databases.
const databaseFilename = 'db.sqlite3';

/// The name of pubspec files.
const pubspecFilename = 'pubspec.yaml';
